// Checks if a string has all unique characters using a Set
function isUniqueLinear(str) {
  // Your code here
}

// Checks if a string has all unique characters without using additional data structures
function isUniqueQuadratic(str) {
  // Your code here
}

// Finds the most frequent element in an array using a hash map
function mostFrequentLinear(arr) {
  // Your code here
}

// Finds the most frequent element in a sorted array
function mostFrequentLogarithmic(arr) {
  // Your code here (remember to sort the array first)
}

module.exports = {
  isUniqueLinear,
  isUniqueQuadratic,
  mostFrequentLinear,
  mostFrequentLogarithmic,
};
